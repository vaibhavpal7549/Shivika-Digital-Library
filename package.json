{
  "name": "shivika-digital-library",
  "version": "1.0.0",
  "description": "Shivika Digital Library - Full-stack Library Seat Booking System",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npx nodemon index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "library",
    "booking",
    "seats"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
